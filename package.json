{
  "name": "samsaadhanii-concordance",
  "version": "0.5.0",
  "description": "A new UI for Samsaadhanii Concordance",
  "source": "src/index.html",
  "scripts": {
    "copy": "mkdir -p dist && cp -r src/assets/ dist/",
    "start": "npm run copy && parcel --port 3000",
    "build": "npm run copy && parcel build"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.8.0",
    "parcel": "^2.8.0",
    "posthtml-beautify": "^0.7.0",
    "posthtml-expressions": "^1.11.0",
    "posthtml-modules": "^0.9.0"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.6",
    "bootstrap": "^5.2.3"
  }
}
